<!DOCTYPE html>
<html class="h-100">
  <head>
    <title>Keeps</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/033affd47f.js" crossorigin="anonymous"></script>   
    
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex h-100 text-white bg-dark">
    <% flash.each do |type, msg| %>
      <div class="alert alert-info alert-dismissible fade show mt-4 alert-fixed" role="alert">
        <%= msg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>
    <div class="container d-flex h-100 p-3 flex-column">
      <%= render 'layouts/header' %>
      <% if user_signed_in? %>  
        <%= yield %>
      <% else %>
        <%= render 'layouts/hero' %>
      <% end %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/masonry-layout@4.2.2/dist/masonry.pkgd.min.js" integrity="sha384-GNFwBvfVxBkLMJpYMOABq3c+d3KnQxudP/mGPkzpZSTYykLBNsZEnG2D9G/X/+7D" crossorigin="anonymous" async></script>
  </body>
</html>
